{
  "name": "Complaints Intake",
  "nodes": [
    {
      "parameters": {
        "content": "## Complaints Intake\n\nThin workflow — logic in lib/",
        "height": 140,
        "width": 360
      },
      "id": "6e546958-215d-4133-a804-f5cdf05eb075",
      "name": "Sticky Note",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -80,
        -120
      ]
    },
    {
      "parameters": {},
      "id": "5cf6bfc3-261f-4ade-94d7-cdc9d7883b9a",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const path = require('path');\nconst fs = require('fs');\nconst { createRequire } = require('module');\nconst req = createRequire(path.join(process.env.N8N_REPO_ROOT ?? 'C:/repos/sandbox-n8n', 'package.json'));\nconst lib = req(path.join(process.env.N8N_REPO_ROOT ?? 'C:/repos/sandbox-n8n', 'lib', 'index.js'));\nconst repoRoot = lib.REPO_ROOT;\nconst dataRoot = lib.DATA_ROOT;\nconst { FileComplaintStore, processComplaintFile } = lib;\nconst fixture = path.join(repoRoot, 'fixtures', 'complaints', 'angry-support.eml');\nconst dest = path.join(dataRoot, 'inbound', 'mailbox', 'angry-support.eml');\nfs.mkdirSync(path.dirname(dest), { recursive: true });\nfs.copyFileSync(fixture, dest);\nconst dbFile = path.join(dataRoot, '_runtime', 'complaints-db.json');\nconst store = new FileComplaintStore(dbFile);\nconst record = await processComplaintFile(dest, store);\nreturn [{ json: record }];"
      },
      "id": "6cd65dd2-589c-4f7d-8e3b-dcd9d28cfefb",
      "name": "Process fixture complaint",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        260,
        0
      ]
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Process fixture complaint",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "complaints-intake",
  "meta": {
    "instanceId": "sandbox-n8n"
  }
}