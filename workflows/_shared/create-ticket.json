{
  "name": "Shared Create Ticket",
  "nodes": [
    {
      "parameters": {
        "content": "## Shared Create Ticket\n\nThin workflow — logic in lib/",
        "height": 140,
        "width": 360
      },
      "id": "a62b658e-4513-4d8a-a818-5664057ea828",
      "name": "Sticky Note",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -80,
        -120
      ]
    },
    {
      "parameters": {},
      "id": "48ce343e-e858-4db6-b34c-f00a6d519d4a",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const path = require('path');\nconst fs = require('fs');\nconst { createRequire } = require('module');\nconst req = createRequire(path.join(process.env.N8N_REPO_ROOT ?? 'C:/repos/sandbox-n8n', 'package.json'));\nconst lib = req(path.join(process.env.N8N_REPO_ROOT ?? 'C:/repos/sandbox-n8n', 'lib', 'index.js'));\nconst repoRoot = lib.REPO_ROOT;\nconst dataRoot = lib.DATA_ROOT;\nconst { createTicketPayload, validateTicketPayload, createTicket } = lib;\nconst payload = createTicketPayload({ title: 'Shared test', description: 'POC', priority: 'medium', source_ref: 'shared_1', source_program: 'servicedesk' });\nconst v = validateTicketPayload(payload);\nif (!v.valid) throw new Error(v.errors.join('; '));\ntry { const t = await createTicket(v.payload); return [{ json: t }]; } catch { return [{ json: { ticket_ref: 'tkt_sim_shared', simulated: true } }]; }"
      },
      "id": "c35ce51f-6c5c-426d-be7b-f7102e275333",
      "name": "Run lib",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        260,
        0
      ]
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Run lib",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "shared-create-ticket",
  "meta": {
    "instanceId": "sandbox-n8n"
  }
}