{
  "name": "sandbox-n8n-platform",
  "version": "3.0.0",
  "description": "Governed n8n operations sandbox POC — native Node runtime, no containers",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
