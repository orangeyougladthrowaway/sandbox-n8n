# Source control

#n8n #governance

POC uses git as canonical workflow source:

- Import: `.\scripts\run.ps1 import-workflows` or n8n GUI
- Export after GUI edits → commit `workflows/**/*.json`

n8n Enterprise Source Control is a future option for bi-directional sync.
